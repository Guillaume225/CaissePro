[project]
name = "report-service"
version = "0.1.0"
description = "CaisseFlow Pro report-service"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "pydantic>=2.8.0"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
