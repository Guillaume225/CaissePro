{
  "name": "@caisseflow/report-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --port 8000",
    "build": "python -m compileall app",
    "test": "python -m pytest",
    "lint": "python -m ruff check app"
  }
}
