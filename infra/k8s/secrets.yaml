# Secrets — values must be base64-encoded in actual deployment.
# Use `kubectl create secret` or sealed-secrets in production.
# This file is a TEMPLATE — do NOT commit real secrets.
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  labels:
    app.kubernetes.io/part-of: caisseflow
type: Opaque
stringData:
  DB_USERNAME: "sa"
  DB_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  JWT_SECRET: "CHANGE_ME_IN_PRODUCTION"
  JWT_REFRESH_SECRET: "CHANGE_ME_IN_PRODUCTION"
  MINIO_ACCESS_KEY: "caisseflow"
  MINIO_SECRET_KEY: "CHANGE_ME_IN_PRODUCTION"
  RABBITMQ_USER: "caisseflow"
  RABBITMQ_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  SMTP_USER: "noreply@caisseflow.com"
  SMTP_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  AI_API_KEY: "CHANGE_ME_IN_PRODUCTION"
